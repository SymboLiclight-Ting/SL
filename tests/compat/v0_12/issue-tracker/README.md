# Issue Tracker

This example demonstrates explicit module imports, enum values, store filtering, and IntentSpec declarations.

```bash
slc check app.sl
slc build app.sl --out build/issue_tracker.py
python build/issue_tracker.py test
```
